[package]
name = "stellar-devkit"
version = "0.1.0"
edition = "2021"
description = "Developer toolkit for testing and simulating the Stellar fee tracker"

[dependencies]
thiserror = "1"

[dev-dependencies]
serde_json = "1"
